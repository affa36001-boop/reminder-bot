# Токен Telegram-бота от @BotFather
BOT_TOKEN=123456:ABC-DEF_your_token_here

# Путь к SQLite базе (или DSN PostgreSQL, например: postgresql+asyncpg://user:pass@host/db)
DATABASE_URL=sqlite+aiosqlite:///./reminders.db

# Часовой пояс по умолчанию для новых пользователей
DEFAULT_TIMEZONE=Europe/Moscow

# Уровень логирования (DEBUG / INFO / WARNING / ERROR)
LOG_LEVEL=INFO

# ── Userbot для учёта лидов (опционально) ──
# API_ID и API_HASH берутся на https://my.telegram.org → API development tools
# TELETHON_SESSION генерируется командой: python generate_session.py
# Если эти три поля пусты — userbot не запускается, бот-напоминалка работает как обычно.
API_ID=
API_HASH=
TELETHON_SESSION=
